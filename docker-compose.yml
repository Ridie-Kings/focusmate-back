version: "3.8"
services:
  mongodb:
    image: mongo
    container_name: sherpapp-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGODB_DATABASE: sherpapp
    volumes:
      - ./mongo:/data/db

volumes:
  mongo-data:
